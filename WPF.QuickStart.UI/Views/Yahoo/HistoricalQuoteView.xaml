<UserControl x:Class="WPF.QuickStart.UI.Views.Yahoo.HistoricalQuoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:WPF.QuickStart.UI.Common.Controls"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
             HorizontalAlignment="Center">

    <StackPanel Height="Auto">

        <TextBlock Text="{Binding DisplayName}" Style="{StaticResource HeaderStyle}"/>

        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="SecurityCodeTxt" Text="Security Code" />
                <ctrl:TextBoxWatermarked x:Name="SecurityCode"  Width="134" ToolTip="Input code. Exple : YHOO, MSCI .." Watermark="YHOO" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock x:Name="StartDateTxt" Text="Start Date" Width="74" />
                <xctk:DateTimePicker x:Name="StartDate" Format="ShortDate" Value="{Binding StartDate}" Width="150" Margin="0 5 10 5" Height="25" />
                <TextBlock x:Name="EndDateTxt" Text="End Date" />
                <xctk:DateTimePicker x:Name="EndDate" Format="ShortDate" Value="{Binding EndDate}" Width="150" Margin="0 5 10 5" Height="25" Tag="" />
            </StackPanel>
            <Button x:Name="Load" Content="Load historical quotes" />
        </StackPanel>

        <Grid Margin="0,10">

            <xcdg:DataGridControl 
                x:Name="_dataGrid" 
                MaxHeight="400"
                MinHeight="400"
                MinWidth="1000"
                Width="Auto"
                ItemsSource="{Binding MyCollectionItems}"/>
        </Grid>

    </StackPanel>

</UserControl>    
